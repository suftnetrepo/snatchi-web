import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'Features', description: 'Explore Snatchi project, scheduling, engineer, document, chat and payment workflows.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
