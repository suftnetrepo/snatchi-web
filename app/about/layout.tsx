import type { Metadata } from 'next';
export const metadata: Metadata = { title: 'About', description: 'Learn why Snatchi was created for audiovisual project teams and who operates the platform.' };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
